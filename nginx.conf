# =============================================================================
# loghub-ui — Nginx: serve o build via HTTPS
# Sem proxy /api: o app chama loghub-api direto via VITE_LOGHUB_API_URL (build-time) + CORS.
# =============================================================================

server {
    listen 80;
    server_name loghub.local;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name loghub.local;

    ssl_certificate     /etc/nginx/certs/loghub.crt;
    ssl_certificate_key /etc/nginx/certs/loghub.key;

    root /usr/share/nginx/html;
    index index.html;

    # SPA — redireciona todas as rotas para o index.html (client-side routing)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
